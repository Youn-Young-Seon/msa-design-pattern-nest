syntax = "proto3";

package user;

service UserService {
    rpc CreateUser (CreateUserRequest) returns (CreateUserResponse);
    rpc GetUsers (GetUserRequest) returns (CreateUserResponse);
    rpc GetUser (GetUserRequest) returns (CreateUserResponse);
    rpc Login (GetUserRequest) returns (LoginResponse);
}

message CreateUserRequest {
    string email = 1;
    string name = 2;
    string pwd = 3;
    
}

message CreateUserResponse {
    message ResponseOrder {
        string productId = 1;
        int32 qty = 2;
        int32 unitPrice = 3;
        int32 totalPrice = 4;
        string createdAt = 5;
        string orderId = 6;
    }

    string email = 1;
    string name = 2;
    string userId = 3;
    repeated ResponseOrder orders = 4;
}

message GetUserRequest {
    string userId = 1;
    optional string pwd = 2;
}

message LoginResponse {
    string token = 1;
}